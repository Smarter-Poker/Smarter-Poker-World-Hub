# Start Here

The current continuation handoff for the `/horses` operator console programme
lives at:

    docs/HANDOFF_CURRENT_STATE.md

Written 2026-09-04. It records what is built, what is live, what is broken, what
is deferred, and exactly what the next agent should do first.

The one-line version: Phases 1, 2 and 3 of 10 are merged and serving; the first
task of the next session is to remove the "AI Model" control from the Settings
tab, because the horses run a deterministic engine and not a language model.

Programme documents: `docs/horses/STABLE-ADMIN-OVERHAUL-PLAN.md` and
`docs/horses/PHASE1-CONTRACTS.md`, `PHASE2-CONTRACTS.md`, `PHASE3-CONTRACTS.md`.
