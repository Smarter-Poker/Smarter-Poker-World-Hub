name: Deploy YT Transcode Worker (Hetzner)

# Auto-deploys scripts/yt-transcode-worker/* to the Hetzner box on every
# push to main. Also runnable manually via workflow_dispatch.
#
# Required repository secrets (set once via GitHub Settings → Secrets):
#   HETZNER_SSH_PRIVATE_KEY  — private key matching the public key in Hetzner Cloud
#   HETZNER_HOST             — public IP or hostname of the Ashburn CX22
#   HETZNER_SSH_USER         — defaults to 'openclaw' if unset
#   SUPABASE_SERVICE_ROLE_KEY — written to /etc/sp-yt-transcode.env on first run
#   YT_GOOGLE_EMAIL           — Google account for yt-dlp cookie auth
#   YT_GOOGLE_PASS            — Google account password
#
# Note: this workflow uses `on: push`, NOT `schedule:`, so it does not violate
# the cron-governance rule in CLAUDE.md §11.5.

on:
  push:
    branches: [main]
    paths:
      - 'scripts/yt-transcode-worker/**'
      - '.github/workflows/deploy-yt-worker.yml'
  workflow_dispatch:

concurrency:
  group: deploy-yt-worker
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.HETZNER_SSH_PRIVATE_KEY }}

      - name: Trust Hetzner host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "${{ secrets.HETZNER_HOST }}" >> ~/.ssh/known_hosts

      - name: Bootstrap (idempotent — apt + pip + dirs)
        env:
          HOST: ${{ secrets.HETZNER_HOST }}
          USER: ${{ secrets.HETZNER_SSH_USER || 'openclaw' }}
        run: |
          ssh "$USER@$HOST" 'bash -s' <<'EOSSH'
          set -e
          # Install once, fast-no-op afterwards
          if ! command -v yt-dlp >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y python3 python3-pip ffmpeg
            sudo pip3 install --upgrade --break-system-packages yt-dlp || sudo pip3 install --upgrade yt-dlp
          fi
          # Always upgrade yt-dlp (YouTube changes their player frequently)
          sudo pip3 install --upgrade --break-system-packages yt-dlp 2>/dev/null \
            || sudo pip3 install --upgrade yt-dlp
          # Install camoufox (stealth Firefox for cookie harvesting)
          if ! python3 -c "import camoufox" 2>/dev/null; then
            sudo pip3 install --break-system-packages "camoufox[geoip]" 2>/dev/null \
              || sudo pip3 install "camoufox[geoip]"
          fi
          # Download camoufox browser binary (idempotent)
          python3 -m camoufox fetch 2>/dev/null || true
          sudo mkdir -p /opt/smarter-poker/yt-transcode-worker
          sudo chown "$(whoami):$(whoami)" /opt/smarter-poker/yt-transcode-worker
          yt-dlp --version
          ffmpeg -version | head -1
          python3 -c "import camoufox; print('camoufox OK')"
          EOSSH

      - name: Sync worker code
        env:
          HOST: ${{ secrets.HETZNER_HOST }}
          USER: ${{ secrets.HETZNER_SSH_USER || 'openclaw' }}
        run: |
          rsync -azv --delete \
            --exclude=node_modules \
            --exclude=DEPLOY.md \
            --exclude=cookies.txt \
            -e "ssh -o StrictHostKeyChecking=accept-new" \
            scripts/yt-transcode-worker/ \
            "$USER@$HOST:/opt/smarter-poker/yt-transcode-worker/"
          ssh "$USER@$HOST" 'cd /opt/smarter-poker/yt-transcode-worker && npm install --omit=dev'

      - name: Write env file (root-owned, 0600)
        env:
          HOST: ${{ secrets.HETZNER_HOST }}
          USER: ${{ secrets.HETZNER_SSH_USER || 'openclaw' }}
          SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          ssh "$USER@$HOST" "sudo tee /etc/sp-yt-transcode.env >/dev/null" <<EOF
          NEXT_PUBLIC_SUPABASE_URL=https://kuklfnapbkmacvwxktbh.supabase.co
          SUPABASE_SERVICE_ROLE_KEY=$SERVICE_KEY
          WORKER_ID=hetzner-ash-yt-01
          MAX_CONCURRENT_YT=6
          EOF
          ssh "$USER@$HOST" 'sudo chmod 600 /etc/sp-yt-transcode.env && sudo chown root:root /etc/sp-yt-transcode.env'

      - name: Install / update systemd units (worker + cookie-refresh timer)
        env:
          HOST: ${{ secrets.HETZNER_HOST }}
          USER: ${{ secrets.HETZNER_SSH_USER || 'openclaw' }}
        run: |
          scp scripts/yt-transcode-worker/sp-yt-transcode.service \
            "$USER@$HOST:/tmp/sp-yt-transcode.service"
          scp scripts/yt-transcode-worker/sp-yt-cookie-refresh.service \
            "$USER@$HOST:/tmp/sp-yt-cookie-refresh.service"
          scp scripts/yt-transcode-worker/sp-yt-cookie-refresh.timer \
            "$USER@$HOST:/tmp/sp-yt-cookie-refresh.timer"
          # 2026-05-12: new keep-alive units that refresh cookie lifetimes
          # WITHOUT re-running the broken camoufox/scrapling headless harvest.
          # See scripts/yt-transcode-worker/keep-alive-cookies.sh for the full
          # rationale. The keep-alive timer fires every 30 min and extends
          # the existing LOGIN_INFO cookies indefinitely.
          scp scripts/yt-transcode-worker/sp-yt-cookie-keepalive.service \
            "$USER@$HOST:/tmp/sp-yt-cookie-keepalive.service"
          scp scripts/yt-transcode-worker/sp-yt-cookie-keepalive.timer \
            "$USER@$HOST:/tmp/sp-yt-cookie-keepalive.timer"
          ssh "$USER@$HOST" '
            sudo mv /tmp/sp-yt-transcode.service /etc/systemd/system/sp-yt-transcode.service
            sudo mv /tmp/sp-yt-cookie-keepalive.service /etc/systemd/system/sp-yt-cookie-keepalive.service
            sudo mv /tmp/sp-yt-cookie-keepalive.timer /etc/systemd/system/sp-yt-cookie-keepalive.timer
            sudo systemctl daemon-reload
            sudo systemctl enable sp-yt-transcode

            # Disable the OLD camoufox harvest timer — it produces anonymous
            # cookies that destroy auth state. The new keep-alive replaces it.
            sudo systemctl disable --now sp-yt-cookie-refresh.timer || true

            # Enable + start the new keep-alive timer. Runs every 30 min.
            sudo systemctl enable --now sp-yt-cookie-keepalive.timer

            # Ensure the keep-alive script is executable on the box
            sudo chmod +x /opt/smarter-poker/yt-transcode-worker/keep-alive-cookies.sh || true

            sudo systemctl restart sp-yt-transcode
            sleep 3
            sudo systemctl is-active sp-yt-transcode
            sudo systemctl is-active sp-yt-cookie-keepalive.timer
          '

      - name: Tail recent logs (sanity check)
        env:
          HOST: ${{ secrets.HETZNER_HOST }}
          USER: ${{ secrets.HETZNER_SSH_USER || 'openclaw' }}
        run: |
          ssh "$USER@$HOST" 'sudo journalctl -u sp-yt-transcode -n 30 --no-pager'
