name: Read Host (NEUTRALISED - delete this file)

# ─────────────────────────────────────────────────────────────────────────────
# NEUTRALISED 2026-08-16.
#
# This workflow used to be:
#
#     run: |
#       echo "HOST: $HOST_SEC" | sed 's/./& /g'
#       echo "USER: $USER_SEC" | sed 's/./& /g'
#     env:
#       HOST_SEC: ${{ secrets.HETZNER_HOST }}
#       USER_SEC: ${{ secrets.HETZNER_SSH_USER }}
#
# Same masking bypass as read_secret.yml: GitHub redacts secrets by matching
# the literal value, and spacing out every character defeats the match. This
# leaked the production host address and the SSH user — the two halves of a
# login target — to anyone able to read an Actions log.
#
# Stripped of every secret reference rather than deleted, because the agent
# GitHub bridge cannot remove files. DELETE THIS FILE.
# ─────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse
        run: |
          echo "This workflow printed HETZNER_HOST and HETZNER_SSH_USER into the"
          echo "build log with GitHub's secret masking deliberately defeated."
          echo "It has been neutralised and should be deleted."
          exit 1
