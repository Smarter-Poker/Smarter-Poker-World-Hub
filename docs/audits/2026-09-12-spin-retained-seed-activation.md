# Retained Spin seed reaches the existing activation authority

An owner whose existing seed already covers the offered stake can reactivate Spins with zero additional seed. The API now permits explicit finite whole zero and preserves existing numeric-string input. Missing, null, blank, boolean and container inputs are refused before activation. The existing database still decides coverage, original source-wallet compatibility, ownership, activation status and positive funding.

The existing actual-route test now runs in the protected Build Safety Gate. Its 70 cases execute the complete handler with auth/database/transport doubles; those responses are not database, funded-game or production proof. Metadata observed at 2026-09-12 05:11:08 UTC matches the previously reviewed function definition and server-only execution grants. No SQL change is included.

Independent candidate review, protected integration, exact served route identity and an owned normal funded route with original receipts/cleanup remain required before delivered acceptance. Full paid Spin draw/play/terminal and cycle recovery requirements stay open in their original delivery homes.
