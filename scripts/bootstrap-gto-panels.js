#!/usr/bin/env node

/**
 * RETIRED: the former bootstrap script sampled legacy strategy_matrix rows,
 * invented additional Grok scenarios, and sent caller-authored actions/EV to
 * a rendering endpoint. It could manufacture solver-looking evidence and is
 * intentionally non-operational. Analysis cards must be projected from sealed
 * Training attempts with validated solver provenance.
 */
console.error(
  'bootstrap-gto-panels is retired: generate analysis artifacts only from sealed, provenance-verified Training evidence.',
);
process.exitCode = 1;
