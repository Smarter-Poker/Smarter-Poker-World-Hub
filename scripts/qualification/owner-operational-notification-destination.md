# Owner operational notification destination — native qualification source

**SOURCE ONLY / UNRUN.** Neither phase has been parsed or executed. No provider,
application catalog, trigger installation, authentication, Realtime delivery,
cache behavior, outbound suppression, deployment or live recovery is qualified by
these files. They authorize no production fixture writes or execution UUID.

This package covers the user's directly reported routing prerequisite: the
owner's operational failures and recoveries must reach task
`01a09b86-5ba8-7290-8657-1041f13dd3ca`, with their original notification evidence
preserved, while ordinary customer, security and accounting notifications retain
their existing destinations and authority. It does not close any financial
original, settle a game or prove membership of FIFO5's historical22.

The implementation owner owns the component, JavaScript gateway, inline/queued
push routing and personal feed/list/count changes. This helper owns only the
adjacent qualification SQL and this contract. Application source tests belong
to the parent package. A source-string assertion or transformed `new Function`
unit harness cannot replace the real loader and connected checks below.

## Inputs and isolated provider

The protected owner must admit a fresh disposable PostgreSQL17 allocation,
database `qual_owner_notify_<execution UUID without hyphens>`, and bind its actual
server image/configuration, source revision, full schema and all dependencies to
the operation. The SQL's database/name/role checks are tripwires, not admission.
Only the protected local execution interface may run the package. Current local
policy and the mandatory hardening standard apply; no GitHub/cloud fallback,
package installation, cron, watcher or background correctness retry is permitted.

Required public inputs, supplied through correctly quoted psql variables:

| Input | Meaning |
| --- | --- |
| `phase` | Exactly `prepare` or `verify`; separate original operations. |
| `execution_uuid` | Actual approved qualification identity, not a self-selected flag. |
| `ordinary_user_uuid` | New admitted fixture principal, distinct from the owner. |
| `legacy_operational_uuid` | New notification UUID owned by this fixture only. |
| `legacy_personal_uuid` | A different new notification UUID owned by this fixture only. |

The owner principal is the exact configured routing UUID
`47965354-0e56-43ef-931c-ddaab82af765`. The provider supplies its isolated
non-secret `auth.users` and `profiles` identity plus the ordinary fixture
principal. Do not copy credentials, production memberships, balances, private
profile data, device subscriptions or financial rows. The script creates neither
principals nor money. Both identity foreign keys must be satisfied through the
admitted provider, never disabled constraints or a replica-role escape.

All phone, SMS, email and push transports must be independently denied outbound
access. No dispatch/cron worker may run. Actual DB mirror outbox writes for
ordinary controls are evidence only and must never reach a real device. The
complete transitive notification trigger closure, including any network extension
or publication side effects, must be inspected and bounded before admission.
Do not replace financial/auth/notification functions with stubs.

Before source installation, capture every affected object: notifications and
destination table columns/defaults/FKs/indexes/ACLs/RLS/policies; view definition
and security-invoker option; original normalization and mirror trigger names,
ordering, WHEN expressions, enablement and function definitions; recorder,
classifier, exact retry and history functions, owners/configurations/ACLs; inbox
constraints and identities; Realtime publication/filter configuration. Recheck
the final installed catalog and continuously exclude concurrent DDL until the
independent readback. The component's recorder MD5 guard is only one dependency.

Retained source evidence includes `screenshot-routing-current-relations-1423.json`
and `screenshot-routing-current-mirror-1428.json` in the operational evidence
directory. The actual recorder full-definition MD5 is
`36601e205494e8768f5a1dce09f4a186`; the captured actual mirror full-definition MD5 is
`a726e393ab7ef02aa7a5a9f0622bee64`. These are `pg_get_functiondef` hashes, not
`prosrc` hashes. The old migration's simpler mirror is not current authority.
Fresh provider/installed composition must bind final candidate bytes and exact
parent-approved object changes; these historical pins alone do not admit a run.

## Finite SQL phases and retained originals

`prepare` runs against the authentic pre-component catalog with empty notification,
inbox and push-outbox tables. It inserts one synthetic owner operational notice
and one ordinary security notice through the real notification triggers, using
the original `_push:none` marker to prevent baseline phone queuing. It records
the operational original through the real recorder, then updates only the
notification's read state and the synthetic inbox investigation fields. This
creates a genuinely earlier immutable inbox snapshot, not a backdated historical
production row or a fake original supplied to the candidate.

Preparation **commits notification evidence in the disposable allocation**. The
executor must retain the original COMMIT command/acknowledgement and a separate
observer's full original notification/inbox rows before applying the component.
If COMMIT response or readback is lost, retain that operation as unknown; do not
rerun preparation or regenerate its UUIDs to erase the uncertainty. No outcome is
inferred from client exit alone.

The protected source installer then applies the real reviewed component. No
preparation trigger is disabled to simulate older records, and this qualification
script does not apply the component itself. Required installation refusal and
rollback/data-retention cases remain part of the component owner's catalog.

`verify` requires those exact two pre-install rows, one original inbox row, zero
outbox rows and no destination rows. It snapshots every selected row, opens an
exercise savepoint, runs the cases below, rolls back to that savepoint and checks
exact whole selected-state equality. It finally rolls back the outer verification
transaction. Sequence allocation is not rolled back and must be recorded
separately. Preparation's committed rows remain until the protected owner disposes
of the **whole allocation**; do not delete immutable evidence or fake cleanup.

## Required SQL observations

1. **Actual original and destination.** Insert raw, inline-marked and in-app-only
   operational notices, failures/recoveries, health messages, Horse Fleet messages
   and structured engine messages through the real `notifications` table.
   The complete persisted original must equal the destination original, including
   every marker, body, read-state and timestamp after existing normalization.
   The exact task/source/event UUID, expected firing/resolved status and eleven
   independent literal name/severity expectations must bind the real positive
   inbox receipts. No related phone outbox row may exist.
2. **Preserved classification.** Null/array data for an explicitly operational
   type still retains the full original. A structured engine alertname must have
   actual JSON string type; numeric/object/null/empty controls must agree with the
   gateway classifier. Ordinary security/customer notices and a matching type
   sent to the other recipient must retain ordinary visibility and mirror behavior.
   An accounting detail remains ordinary but follows its existing non-push rule.
3. **Atomic original failure.** Keep the duplicate-identity refusal separate from
   a fresh original's actual `notifications_actor_id_fkey` refusal. The latter
   supplies an actor UUID proven absent from fixture profiles, so capture runs
   before the actual original FK fails23503. Neither its destination nor its inbox
   key may survive. A fresh NOT NULL failure is a separate negative control.
   Do not swallow unrelated SQL errors and count them as the expected refusal.
4. **Actual caller permissions.** Run direct-table/view queries as authenticated
   owner and other principal with the real `auth.uid()` function; do not replace
   it. Operational originals are hidden only from personal reads, ordinary owner
   notices remain visible, and cross-user reads remain denied. The service role
   must use the personal view despite its RLS bypass, and must not gain private
   destination UPDATE or private recorder execution. Pure classifier execution
   required by actual trigger WHEN predicates must work under each permitted role.
5. **Personal relation/count contract.** Under the actual service role, compare
   view rows and the feed/list/unread relational filter, including the two read
   flags and the `accounting_invoice_detail` exclusion. This proves SQL behavior,
   not that the deployed routes actually select that relation.
6. **Historical capture and replay.** Directly invoke the bounded explicit history
   intake once. It must attach the old inbox ID without changing its original
   payload, first-received time, status, severity or investigation. Only recorder
   delivery bookkeeping may advance. The destination preserves the actual current
   notification, while the earlier inbox snapshot may differ only in the four
   reviewed read/timestamp fields. Ordinary history and every notification field
   remain unchanged. A repeated completed call must report no remaining work.
7. **Real pending recorder refusal and exact retry.** Inside the disposable test
   transaction only, add a CHECK that rejects one newly generated event key on
   the actual inbox table. The actual recorder must fail with23514 while the
   destination retains the complete original and no phone outbox row appears.
   Remove only that test constraint, then invoke the service-role exact-original
   retry once and verify one canonical ID and unchanged evidence. This is
   deliberate isolated fault injection, not a new recorder or runtime constraint.
   A262145-character body exercises the actual256KiB inbox constraint separately:
   the complete original remains pending and unchanged; it is never truncated or
   treated as delivered. It is not a claim that this permanent refusal recovers.
8. **Collision and malformed receipts.** Corrupt only generated fixture copies,
   one isolated subtransaction per control: wrong name/status/severity/task,
   changed original body, wrong already-populated receipt pointer and malformed
   original identity. Strict readback must clear false acknowledgement, preserve
   evidence and return explicit pending/failure. Each corruption rolls back; exact
   originals and canonical rows must restore. Do not count source name or a
   positive integer alone as a receipt.
9. **Accounting authority remains intact.** A raw `accounting_invoice` without
   its canonical invoice/message/delivery linkage must still fail23514 with exact
   message `accounting_push_delivery_link_missing`, even with
   `_push:none`; that marker cannot manufacture a delivery receipt. A genuine
   linked invoice positive control requires the accounting owner's approved
   provider and full actual receipt. This script does not fabricate financial
   entities to get an unrelated positive case.

The pure classifier and `fn_retry_owner_notification_destination(uuid)` are
finite owning-request dependencies. The latter must return exact
`{notification_id,target_task_id,inbox_event_id}`; a null inbox ID stays pending.
`fn_capture_owner_notification_history(integer)` is explicit bounded historical
intake only and returns `candidates_seen`, `inbox_receipts_recorded`, `pending`
and `uncaptured`. No scheduled background reconciler may supply missing
correctness. A destination that cannot be recorded remains visibly pending;
neither this fixture nor a timer turns it into an acknowledged inbox event.

## Connected and interruption gates still required

The following remain mandatory and **UNRUN**; SQL role simulation cannot replace
them:

- Real authenticated PostgREST direct-table and view requests with independently
  issued fixture sessions for both principals, plus unauthorized/expired-session
  controls. No protected token values in transcripts or source.
- Actual Realtime subscriptions to the direct notifications table using the
  real publication, filters and RLS policy. Establish acknowledged subscriptions
  and ordinary positive controls, commit operational and ordinary notifications,
  and observe the exact ordinary IDs while the operational IDs remain absent.
  Require the full committed barrier and bounded observation window; an unopened
  subscription or missing all events is failure, not suppression proof. Also
  qualify reconnect and an already-open owner subscription during historical
  destination backfill; existing rendered/cache content must be cleared safely.
- The actual installed Node/Next loader and canonical `notify` module graph,
  actual Supabase client and real routes. Exercise withPush true/false, raw inserts,
  original persistence error, missing/null/malformed destination receipt,
  transient pending then one exact retry, retained permanent pending and lost
  acknowledgement. No operational path may reach any phone transport. Observe
  ordinary transport intent only in the isolated outbound-denied fixture.
- Invoke actual authenticated `/api/notifications/feed`,
  `/api/notifications/list`, `/api/notifications/unread-count`,
  `/api/user/get-header-stats`, the bell and notification page. Verify exact IDs/counts and
  errors across initial load, cached load, private HTTP cache, client store,
  refresh/reconnect and existing loaded content. A cached owner notice must not
  persist after the routing transition. An absent view or permission error must
  remain an error, never fall back to an unrestricted table or fake empty success.
- Simultaneous insert/retry/history requests for the same generated UUID, duplicate
  caller delivery, ambiguous INSERT/COMMIT response, cancelled client, DB restart,
  destination persistence failure, permanent oversize, bounded-history limits and
  an already-populated wrong pointer. Preserve actual original identities and
  pending/unknown states; obtain authoritative readback before any effectful retry.
- Exact install refusal/replay/drift and rollback/data-retention cases against the
  current full catalog. Reverting JS or dropping a view does not undo committed
  destination data or safely restore personal delivery. Preserve all historical
  originals and investigation dispositions; no acknowledgement backfill by label.

Use the existing directly triggered required catalog, with all changed dependency
paths bound to the final commit. No periodic check or release watcher. A collector
must reject missing required cases, incompatible authority, interrupted execution,
unobserved original workers or absent whole-allocation teardown. A successful SQL
exit alone is not full qualification, no-phone proof or release authorization.

Each statement requests8seconds, lock waits2seconds and idle transactions20seconds.
Nested function settings can change effective deadlines; the executor must qualify
actual PostgreSQL behavior and independently enforce a finite whole-operation
deadline and resource allocation. The SQL begins with bounded row counts and a
single intentionally oversized original; those are not a physical memory bound.
Retain ordered original outputs/SQLSTATEs, catalog/source hashes, every fixture ID,
phase COMMIT/rollback boundaries, stopped backends and independent final teardown.

The final live gate requires separately approved publication of the whole database,
gateway, mirror and personal-consumer composition, served revision readback and a
genuine authorized operational original reaching this task with no owner push or
personal-feed copy. Preserve customer/security/accounting delivery. Do not send
production probe alerts or infer recovery from source custody. Until these facts
exist, the routing prerequisite remains open.
