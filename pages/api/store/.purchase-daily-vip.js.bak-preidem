// STALE ARTIFACT - safe to delete.
// Temporary pre-edit backup of purchase-daily-vip.js made 2026-08-03.
// Contents removed; the original is in git history. Not a Next.js route
// (dotfile + non-.js extension, so the pages scanner ignores it).
// rm was blocked on this mount, so it could not be deleted automatically.
