import React from 'react';
// Intentional unused import to trigger a build warning/error
import { NotReal } from '../src/lib/doesNotExist';

export default function AutofixE2ETest() {
  return <div>Autofix E2E Test Page</div>;
}
