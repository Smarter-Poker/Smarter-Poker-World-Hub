# Messenger Club And Invoice Workspaces

The live Messenger reopened in a persisted club identity even when a person visited its ordinary social entry. Invoice conversations appeared among ordinary conversations. The notification deep link selected a conversation without loading its messages, leaving a real issued invoice behind an empty chat panel.

This change makes ordinary entry social. Only server-confirmed, active club memberships populate the Club Arena widget. Clicking it selects a club and exposes separate Messages and Invoices tabs. Owning a social page or supplying a club URL does not grant membership, and an ordinary member does not gain a club representative identity. The same authorized workspace serves first load, refresh and search. An accounting classification outage returns an error instead of placing financial conversations in a social list. Account and workspace generations reject delayed responses, including a switch away and back. The former unscoped persisted conversation list is no longer painted during authentication.

Invoice notifications and embedded conversation commands use the same authenticated resolver and message-loading path. Accounting conversations have document cards, current payment status from the linked invoice, and no person-profile or voice/video-call header. Invoice figures remain immutable. Equal-time message pagination now carries the message ID and preserves PostgreSQL microseconds.

The club inbox excludes separate issuer-to-agent invoice threads. Consolidated weekly accounting documents have a dedicated card with rake earned, union receipts and club payouts. Database publication of the consolidated weekly document, treatment of already delivered individual sender receipts, and the original waterfall reconciliation remain in the associated Club Arena accounting work. Their completion is not claimed by this UI change.

Validation: 15 authorization/workspace contracts and 8 rendered/interacted component contracts passed with existing dependencies. The actual helper also read the production account: 8 social conversations with no accounting conversations; Club JAQK ordinary messages with no accounting conversations; two recipient accounting threads in its Invoices tab; the observed invoice notification resolved into that tab. These were read-only service-backed checks, not deployed UI verification. JSX parse and undefined-name/hook checks passed. CI, merge, publisher provenance and a new live browser pass remain required.


Weekly summaries and archive integration: the message API now uses the installed participant-scoped page reader, which verifies invoice provenance and current status and filters archived club payout copies before pagination. The notification feed and list exclude those archived details, and unread counts match the existing rule that neither read flag is true. Database outages no longer pretend to be an empty successful notification list. Genuine human invoice replies remain visible to the issuer in the Invoices tab. Club managers receive one read-only weekly preview while reconciliation is outstanding; individual ledger IDs are omitted, unknown roles stay explicit, and downstream redistribution stays separate. Ordinary players cannot retrieve club totals. Thirty-two native workspace/render checks passed; live read-only helper calls confirmed the existing owner's social inbox contains no accounting threads and club invoices resolve to the separate folder. Two club-summary migrations are installed and 26 PostgreSQL fixture assertions passed. Full deployed browser proof and the financial waterfall reconciliation remain outstanding.

Receipt visibility follow-up: production build d907420c2edc4d9127786b6384884fd6d06f50e5 was verified through the health endpoint and the authenticated browser. Social entry showed the collapsed Club Arena widget, and the club Invoices tab showed the weekly reconciliation preview. That pass exposed an empty legacy club thread whose only invoices had been archived. The inbox and notification resolver now use the installed participant-scoped receipt visibility reader, hide that empty thread, and derive previews from visible messages. Workspace changes preserve the surrounding Messenger while loading. All 36 native workspace, component and message API checks pass. Read-only calls with this helper return eight social threads with no invoices and one visible Club JAQK invoice thread. This follow-up still requires its own CI, merge and deployed browser pass. The failed historical waterfall remains unresolved and is not certified by Messenger verification.


Authenticated invoice opening also exposed a second social identity card inside the message history, separate from the corrected header. It linked a synthetic accounting conversation to `/hub/user/undefined`. Accounting histories now render their own document introduction, and social profile links require a real username. The empty accounting state also describes documents and discussions. The additional rendered regression assertion brings the native total to 37.


The authenticated notification click selected the correct invoice, but the native notification overlay obscured the destination. The global overlay now dismisses only after successful route completion while notifications are open; failed navigation retains it, and other overlays do not subscribe. This preserves the existing no-navigation behavior when merely opening notifications. Two additional interaction checks bring the native total to 39.


Notification visibility was also enforced at the browser boundary. Legacy cache arrays without the current version and authenticated owner are rejected; cache age cannot be extended by marking a row read. Late responses from a previous account cannot replace the current feed. Archived individual club copies are filtered from cache, fetch, live insertion and final render, while archive updates remove an already-open copy. These checks preserve ordinary social alerts and each payee's own invoice notification. The native accounting UI/workspace suite now has 41 passing checks.


The retained Messenger shell no longer labels a loading or failed inbox as empty or shows a stale weekly preview during that request. The explicit loading/error status remains visible until the requested inbox is authoritative.


Search visibility follow-up: both search endpoints previously read social_messages directly, allowing archived individual invoice content to escape the invoice-page filter. Both now use the same authenticated workspace resolver and the installed fn_messenger_search_messages reader. A request without a workspace searches social messages only. Conversation search rechecks active club membership and visible receipt/discussion access. The database excludes archived rows before the result cap, preserves literal percent/underscore searches, and verifies invoice identity and current status from the receipt link. Migration 20260914132940_invoice_search_only_returns_visible_verified_messages is installed; 18 new native PostgreSQL assertions passed alongside the prior 32. Forty-seven focused WH tests, including both real API handler auth/refusal wiring, pass; the existing 51 notification and overlay checks also pass. Search deployment and live verification follow this commit.


### Search response lifecycle follow-up

A conversation switch could retain previous search results, and failed sidebar searches were presented as zero matches. Both search views now use one scoped request hook. The current actor, workspace, conversation and query identify each result; a scope change hides old results in the first render and cancels late completion. Failed or malformed responses show an unavailable state. Conversation selection resets its search UI; sidebar hits also require a currently visible conversation. Five asynchronous lifecycle regression checks pass, bringing focused coverage to 52. This follow-up still requires merge, production revision and browser verification.

Rendered production inspection also found invoice conversation titles inheriting the outer dark page text color on a light Messenger panel. The conversation header and accounting introduction now explicitly use the Messenger theme text color; both light and dark component variants are covered. Focused coverage is 53 passing checks.

### Push health exact-count follow-up (not released)

Read-only production audit at 13:45 UTC found the dispatcher healthy and VAPID configured with matching keys, but only one of three active devices confirmed a display receipt. The dashboard's seven-day counts were sampled through row-limited PostgREST reads and omitted today's accounting traffic; unchecked database errors could render healthy zero totals.

The push-health API now consumes one admin-authorized database snapshot, rejects errors/missing or inconsistent data with 503, and never falls back to sampled counts. The page validates the snapshot, identifies its observation time, counts both pending and processing backlog, and keeps display confirmation separate from subscription existence. Eight API/schema/render tests passed, including forged actor input, unavailable data, more than 6000 accounting rows, receipt requirements and truthful unavailable rendering.

Required reader fn_push_health_snapshot(uuid) is staged separately for the Club Arena database owner. It fixes the grouping window to seven days, computes exact aggregates in PostgreSQL, preserves all-age unfinished backlog, scopes send-rate calculations to one cohort, and requires the authenticated actor (or trusted service caller on behalf of that actor) to be an administrator. Its 24 native PostgreSQL checks pass. The caller must not release before the owner installs/readbacks the guarded reader. No push or notification was sent as a test. GitHub billing currently blocks normal release checks for PR1797 as well.

The push-health page's original one-time request could outlive an account switch or component unmount. Its scoped reader now clears the previous identity immediately, observes same-tab auth and cross-tab storage changes, ignores stale responses, and unsubscribes on unmount. Four additional lifecycle tests pass (12 push-health tests total). The cumulative failed count is explicitly labeled All Time; seven-day grouping and 24-hour funnel labels remain distinct. SQL contract unchanged.

The push-health reader was installed by the database owner as 20260914140459, MD5 7a869169a979bdd444ca2ecfac746f97; live readback remains receipt-based (3 pushed devices, 1 confirmed). The caller dependency is ready. Health lifecycle coverage is 13 tests including SDK hydration. A club invoice entry URL is now supported: /hub/messenger?clubId=<club UUID>&folder=invoices. It resolves only among server-confirmed joined clubs, preserves explicit conversation links, and defaults unsupported folders to Messages. Native route coverage passed; deployed route proof remains pending.

Cold invoice URL follow-up: the conversation link was marked handled before its verified response was accepted. An initialization scope change discarded that response but prevented another attempt. The scoped link hook now marks acceptance only after current-scope and complete-payload checks, retries on a new scope, and rejects delayed account/unmount responses. Seven native link regressions exercise the actual page response handler; combined workspace, accounting UI, search lifecycle, cold-link and push-health coverage is 74 passing checks. A fresh production invoice URL remained in social Messenger before this source change; post-deploy cold-route proof remains required. The local build stops in prebuild because this worktree has no node_modules and copy-tesseract-assets requires it; shared dependencies were used read-only for the targeted tests, with no install or dependency linking.

The final live screenshot confirmed readable invoice headers and result rows but exposed another inherited white-text input on the light conversation search panel. The input now explicitly uses the active Messenger text color; its search and result contracts are unchanged.
