# Restrict the exact Diamond credit EXECUTE grant

The existing `balance_rpcs_not_executable_by_clients` economy invariant stopped PR1743's CHECK10. Root's 2026-09-12 04:13:43 UTC catalog receipt identified one violating grant in its four-authority set: authenticated EXECUTE on `public.add_diamonds_to_balance(uuid,integer,text,text,text,uuid)`. Anon lacked access; owner and service had access. The other three captured authorities were already service/owner-only. CHECK8 had not run. The CI invariant and runner have not been changed.

The function's definition MD5 `5d356b15727558df9c498dbb05a745d0` matches Diamond's existing full196-line receipt from2026-09-11 23:01:38 UTC. That body mutates balances/journal and retires debt, but existing profile and journal guards remain independent protections. The grant mismatch does not establish an unauthorized successful credit, duplicate payment or loss. This joins the existing Diamond G04 authority/retention assessment; it is not a second credit implementation.

`20260912042515_restrict_diamond_credit_execute.sql` is a TIER2 ACL-only migration. It verifies the exact six-argument definition, owner, definer setting, search path and complete observed ACL, removes only authenticated EXECUTE with RESTRICT, then requires exact owner/service ACL and no effective anon/authenticated access. Both assertions accept only the expected state; body/settings/owner/ACL or inherited-role drift aborts. The exact already-revoked state is allowed for an idempotent administrative rerun. No function body, amount, signature, business row, other overload, default privilege, owner or service grant changes.

G8-URGENT-DIAMOND-EXECUTE-0001 and its reserved-path addendum authorize this exact migration and audit file only, on the existing `fix/gdpr-respects-financial-refusal` worktree at parent `9ddcbc23caba1b09909c42bee4854aee20537969`. The five committed GDPR files remain unchanged. G8 owns independent candidate acceptance, production preflight/application, commit/push and protected release. The generic repository instruction to apply/push immediately does not override that program checkpoint. No production application, commit or push has been performed by this implementation step.

The standard migration-safety template and workflow were read and followed for candidate preparation. This is an access revocation, not a player-specific monetary correction, DROP, type/signature change or balance backfill. It therefore uses the explicitly authorized TIER2 header and transactional assertions, not a broader Tier3 financial mutation. RLS scope and table-size checks do not apply to this ACL-only change. Root supplied the exact overload/dependency preflight at04:24:23 UTC and security/performance advisor baselines; no advisor or business query was repeated here. The REVOKE itself does not request a schema-cache reload; the migration tool's history-table DDL may still reload the cache, so root must honor the shared window.

The canonical Club Arena reservation helper was recovered unchanged from its cached origin/main because its working-tree file was absent. Run from the owned Hub worktree with its documented skip-fetch option, it reserved042515 rather than the initially proposed042500; G8 approved the actual path before candidate SQL was written. An additional filename census checked193 available of201 Hub trees and56 available of58 Club Arena trees, plus both cached origin/main migration inventories. Only the owned reservation matched042515. The ten unavailable trees, other machines and later remote changes remain explicit final preflight qualifications. No shared checkout/index/ref was modified to reserve the name.

Root's affected-caller review reports no required direct authenticated dependency. Existing service calls keep their explicit grant, and postgres SECURITY DEFINER wrappers keep owner authority for nested calls. The invoker trigger text match is only a RAISE HINT, not a live lower invocation. Current owner/service caller authorization remains separate from this lower permission correction; a lexical source census alone is not served-role proof.

## Isolated PostgreSQL17 validation

The existing Homebrew PostgreSQL17.11 binaries were used with a private temporary cluster, no TCP listener, a private Unix socket, isolated fixture roles and no credential files. The exact saved196-line definition was installed with normal function-body checking. Its resulting catalog MD5 matches the reviewed body. The function was never invoked. No business relation or player row was created; only fixture roles, functions, grants and empty databases were used.

The actual candidate bytes passed all18 checks: observed client privilege mismatch; SQL parsing and exact revoke; owner/service/body/OID/settings preservation; repeat application; atomic refusal for body, extra ACL, PUBLIC, grant-option, missing-service, search-path and owner drift; refusal of inherited authenticated and anon access; rollback of the direct revoke after the inherited authenticated postcondition fails; absent exact overload refusal; and preservation of a distinct overload's body and grants. These checks exercise the migration's real pre/post blocks on PostgreSQL, not a model of the SQL.

Initial sandbox setup attempts failed before database cases because the environment blocked a small SysV shared-memory segment, including with explicit mmap/posix settings. A separately preserved fixture setup attempt exposed a missing statement separator after pg_get_functiondef output. The fixture now adds a separator outside the unchanged definition. These are recorded setup limitations, not candidate SQL failures. The final run used permitted local process execution, passed18 checks and stopped its server. No installed dependency, application test or production/provider operation was introduced.

Evidence is sealed in the G5 coordination packet `work/g5/diamond-credit-execute-implementation-0001`, including exact candidate, saved definition identity, local harness, all logs, catalog before/after states, version checks and policy/preflight references. This validates catalog permission behavior only. It does not certify dependent economic bodies, real JWT/PostgREST roles, full production triggers, deployed business flows, or the overall foundation.

Before application, G8 must obtain the independent exact-candidate decision and refresh current source/ACL/version applicability. After application, record the migration and fresh exact definition/ACL/effective privileges, compare advisor results, and run the unchanged protected CHECK10 and CHECK8 suite through the normal release lane. Final main/deployment identity and served proof remain required. No gate bypass is authorized.

Failed migration assertions roll the transaction back. After a committed revoke, prefer a forward correction that keeps direct client access closed. Regranting authenticated EXECUTE restores the known invariant violation and requires a separate explicit G8 decision with actual caller evidence. There is no automatic rollback grant; reverting the unrelated GDPR application change does not require it.
