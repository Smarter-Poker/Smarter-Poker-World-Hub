# Private invoice readers and notification routing source successor

Status: source only, unrun, uninstalled and unpublished. The September 14 protected local pipeline policy governs this work. No tests, builds, direct database operations, GitHub operations or provider test sends were performed. The sealed predecessor is `a3d204507053b4ca6c4f98ee477df83525ea1061`.

The invoice inbox rejected a club sender opening a private recipient thread before a discussion, but message paging checked participation alone. The club owner is a participant in the payee's actual invoice conversation. Direct page/search RPCs and the captured message-table policy also admitted that participant, and club-admin invoice-table access bypassed the hidden inbox.

`get-messages` now shares workspace resolution with links and search. All three Messenger reader calls use explicit private-contract entry names. Missing contracts fail closed; there is no participant-only fallback. Club Arena owns the corresponding guarded SQL candidate. It restricts verified posted club-to-agent/player rakeback and commission attachments to their actual payee, retains genuine discussion, hardens legacy page/search/thread readers, restricts direct message/invoice/delivery/notification reads, and neutralizes shared stored accounting previews. The existing invoice, in-app notification and durable push transaction is unchanged.

The push worker now compares the complete destination, including conversation and folder query parameters. Failed window navigation opens the requested invoice. Its minimal notification fallback retains routing data and the per-receipt tag.

The next source follow-up makes all accounting OS banners generic at the canonical dispatcher and both Web Push/FCM serialization boundaries. The formatter allows only the receipt identity and navigation fields, uses fixed application icons, and discards financial text, custom images/actions, nested private data and account badge counts. Both push-capable worker sources (`public/push/sw.js` and root `worker/index.js`) also sanitize older incoming accounting payloads before normal and fallback display. The root worker receives the same exact-query navigation and quiet receipt replacement behavior. Private invoice text remains in the existing stored invoice/notification, and no queue, preference or financial writer changes. New behavioral source cases cover both worker paths and both actual transport formatters. They are UNRUN. Generic banners limit content exposure; they do not supply the still-missing account/epoch binding or per-invoice device acknowledgment proof.

Push enrollment previously ignored incumbent lookup/retirement errors and wrote ownership and preferences separately. The API now requires the service-only `fn_change_push_subscription_ownership(uuid,jsonb,boolean)` receipt. Club Arena's candidate serializes enrollment and retirement, verifies stored possession, keeps the existing per-user/device index and adds one active owner per endpoint. A failed mutation rolls back the whole transaction. The caller returns unavailable if the contract is absent, malformed or uncertain. Courtesy notices are sent only for the committed displaced-recipient list; repeated enrollment returns no repeated displaced owner.

Source regressions cover hidden issuer paging/search, preserved discussion, missing database contracts, caller identity/cursors, query-only push destinations, cold/refused navigation, fallback tags, atomic subscription result validation and refusal. Companion PostgreSQL sources use actual role/JWT readers, direct table policies, a real late-write failure and two concurrent sessions. All are **UNRUN** and require protected admission.

The follow-up removes the fallback's union-only period filter. An unresolved standalone book now uses the exact recorded club/period through `fn_messenger_private_weekly_summary`, an actor-bound STABLE wrapper around the canonical v3 summary. The wrapper checks actual statement recipients and active membership in the same snapshot as its report; the server validates the returned actor, club, period, scope, bounds, version and status. No current union membership determines historical accounting scope. The wrapper exposes scalar weekly totals rather than source IDs or per-recipient diagnostics. Completed statements are not duplicated; unknown and unresolved amounts are never promoted to paid. Ambiguous books ending together fail closed until a canonical combined statement is available. Source regression cases cover standalone, former-union, cross-club, wrong-actor/period/scope/version, unavailable contract and completed-statement behavior. All are UNRUN.

Outstanding: the browser worker has no trusted current-account/epoch binding. A provider message already in flight cannot be recalled by subscription retirement, and the current payload carries no recipient identity for display filtering. Stored endpoint exclusivity therefore does not establish complete cross-account device isolation. The companion account-bound display contract proposes integration into the existing auth, ownership and delivery paths, with generic financial banners and explicit refusal/retry semantics. It is not implemented. Root captured fresh Messenger reader and notification-preference catalog metadata at 2026-09-15T02:37:55.2878Z; portable fixture integration and protected execution remain pending, along with broader privileged-reader review, deployment, and browser/provider acceptance.
