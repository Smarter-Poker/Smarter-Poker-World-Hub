# 2026-09-04 - The Mint: audited and hardened (World Hub half)

Dan, 19:25 UTC: audit the Mint and improve, upgrade, enhance and optimize everything inside it. The database half landed in Club Arena as migration `20260904194036_the_mint_hardened` (branch `fix/the-mint-hardened`; full audit in that repo's `docs/changelog/2026-09-04-the-mint-hardened.md`). In short: `ca_mint_ledger` is append-only with one row per journal leg; `fn_ca_mint` / `fn_ca_burn` link their leg by idempotency key; an issuance ceiling (`ca_mint_policy`: 10,000,000 per operation, 25,000,000 per rolling 24h for chips) is refused in the door with a readable reason and again at commit for every door; `fn_ca_mint_overview` reconciles the register against the supply meter as of the meter's snapshot; every register row carries `origin`; the free mint `mint_club_chips` (only ever referenced by the unreachable legacy block in `pages/api/club-arena/mint-chips.js`) and `mint_club_promo` are closed.

This repo:

- `pages/api/horses/mint.js`: the overview passes the new `reconciliation`, `issuance`, `policy` and `by_origin` keys through (add, never rename); the ledger reads `origin` and accepts `?origin=`; the two policy refusals (`amount_over_the_single_mint_cap`, `over_the_rolling_24h_issuance_ceiling`) have sentences; the header no longer claims issued and circulating are not supposed to match.
- `pages/horses/index.js`, the Mint tab: a card that says in one line whether every chip is accounted for (register at the meter's snapshot, meter, difference, drift since baseline, snapshot age; green when the difference is the meter's own drift and nothing else), an Issuance Policy card (both caps, 24h issued and retired, headroom with percent used, issued and retired since the baseline, the policy note), an Origin filter and column on the ledger (and in the CSV), and the stale "not supposed to match yet" copy removed.

Verified: `node --test __tests__/horses-reverify-routes.test.mjs __tests__/horses-phase2-server.test.mjs` 122 pass / 0 fail; eslint on both files 0 errors. Production after the migration: register 193,144,847.90 = register at meter, meter 193,144,881.42, difference 33.52 = unexplained since baseline 33.52.
