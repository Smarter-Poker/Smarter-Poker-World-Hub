import React from 'react';
import ReactDOM from 'react-dom/client';
import HorsesAdmin from './HorsesAdmin.jsx';

ReactDOM.createRoot(document.getElementById('horses-root')).render(
    <React.StrictMode>
        <HorsesAdmin />
    </React.StrictMode>
);
