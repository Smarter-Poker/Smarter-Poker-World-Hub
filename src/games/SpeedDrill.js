/*
 * DEAD FILE — do not import.
 *
 * This module was a stale duplicate of the Speed Drill game that still
 * carried the reveal-contradiction bug fixed in src/games/SpeedDrillGame.js
 * (the reveal UI contradicted the mixed-frequency grader). Nothing in the
 * repo imports it (verified by repo-wide grep 2026-08-08); the live game is
 * loaded via dynamic import of src/games/SpeedDrillGame.js in
 * pages/hub/memory-games.js.
 *
 * It was neutralized in place because this sandbox cannot delete files.
 * It is safe to `git rm src/games/SpeedDrill.js` from a normal shell.
 */
throw new Error(
    'src/games/SpeedDrill.js is a dead duplicate — import src/games/SpeedDrillGame.js instead.'
);
