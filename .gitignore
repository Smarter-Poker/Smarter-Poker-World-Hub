
# Editor / sandbox backup files
*.bak
*.orig

!.venv/.gitkeep
!.vscode/extensions.json
!data/bravo-logs/.gitkeep
!data/charity-scraper-logs/.gitkeep
!data/pokeratlas-logs/.gitkeep
!data/scrape-evidence/.gitkeep
!data/tournament-logs/.gitkeep
# Agent brain artifacts (may contain tokens from browser sessions)
# Auto-generated service worker files (next-pwa / workbox)
# Background shell state
# Charity scraper logs — keep dir, ignore contents
# Club Arena build artifacts — DO NOT gitignore!
# Content engine
# Credentials (keep secure)
# Deploy audit trail
# Editor directories and files
# Ephemeral daemon state logs — keep dir, ignore contents
# Keep directories via .gitkeep — ignore contents only
# Legacy one-off scripts, logs, SQL moved from root
# Local deployment logs and temporary files
# Logs
# NEVER manually add files to public/hub/club-arena/.
# Next.js build artifacts
# Poker Brain dev assets (reference screenshots, debug crops)
# Python bytecode
# Scraper daemon outputs (data goes to Supabase, these are ephemeral local audit trails)
# Soft-deleted paths (sandboxed delete tombstones)
# Source maps inside public/ — uploaded to Sentry at build, not shipped to browsers via git
# Use scripts/build-club-arena.sh to rebuild — it atomically
# Vercel deploys from git, so these MUST be committed.
# Virtiofs EPERM workaround: stale rename tombstones (safe to ignore; cleaned up outside git)
# cleans old hashes, copies new build, and commits+pushes.
**/*.dead.*/
**/*.dead/
**/*.stale.*/
*.dead
*.dead.*
*.env.*
*.local
*.log
*.njsproj
*.ntvs*
*.pyc
*.sln
*.stale.*
*.suo
*.sw?
.DS_Store
.agent/skills/credentials/
.agent/skills/deployment.md
.bg-shell/
.env
.env*.local
.env.production
.gemini/
.idea
.memory/
.memory/**
.next/
.venv
.vercel
.vscode/*
__pycache__/
_legacy_club_arena_standalone_pages.dead/
_legacy_logs/
_legacy_scripts/
_legacy_sql/
data/bravo-live-snapshot.json
data/bravo-logs/*
data/charity-scraper-logs/*
data/pokeratlas-live-snapshot.json
data/pokeratlas-logs/*
data/scrape-evidence/*
data/tournament-logs/*
dist
dist-ssr
git_status_all.txt
lerna-debug.log*
logs
logs/
node_modules
.node_modules_safe
.node_modules_old_*/
npm-debug.log*
pnpm-debug.log*
pokerbrain-assets/
public/hub/club-arena/assets/*.css.map
public/hub/club-arena/assets/*.js.map
public/sw.js
public/sw.js.map
public/workbox-*.js
public/workbox-*.js.map
public/worker-*.js
scripts/poker-brain/.venv/
src/content-engine/node_modules
vercel_deploy_output.txt
yarn-debug.log*
yarn-error.log*

# Agent scratch summaries (Anthropic Claude / Antigravity sessions) — never commit
summary_pt*.md
summary*.md
user_messages.txt

# Scratch/quarantine dir — lock files and dead code get parked here by
# agents and by git-safe-push recovery. Never commit it (2026-07-25).
_to_delete/

# Source tarball accidentally committed 2026-07-25 — build artifact, never track
wh-fixes.tar.gz
*.tar.gz
trivia-*.tgz
tmp/*.tgz

# ── U4/audit probe leftovers (2026-08-12) ───────────────────────────────
# Throwaway files from verifying the CI gates and lint passes. Each one was
# used to prove a checker actually trips (inject a fake dead route / phantom
# table / stranded table, confirm it fails, confirm it goes green again) and
# then emptied.
#
# They are ignored rather than deleted because the agent mount forbids
# unlink() — they could only be truncated. Ignoring them matters: this repo
# is worked by several agents and a `git add -A` from any of them would
# otherwise commit an empty .mjs at the repo root and a probe service into
# src/services, which the stranded-writer gate scans.
#
# Safe to `git rm`/delete by hand at any time.
.t[0-9a-z].config.mjs
.tmp2.config.mjs
.tmp-audit-flat.config.mjs
src/components/__canary_route_probe.jsx
src/__u4probe/
src/services/__U4ProbeService.js
data/tour-logs/*
